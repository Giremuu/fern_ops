[defaults]
inventory         = inventory/hosts.ini
remote_user       = ubuntu
host_key_checking = False
roles_path        = roles
callbacks_enabled = timer, profile_tasks

[ssh_connection]
pipelining = True